test:
  override:
    - echo "test"

deployment:
  master:
    branch: master
    commands:
      - aws s3 cp --recursive templates/ s3://gitshame/templates
      - ./scripts/lambda-deploy make-page
      - ./scripts/lambda-deploy pygmentize
